Simple interface to create streams
==================================

Some hack with python. No comments please.

Usage
-----

    pip install -r requirements.txt gunicorn
    python -m streaminterface

